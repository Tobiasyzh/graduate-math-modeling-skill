# 研究生数学建模全流程 Skill

这是我为研究生数学建模比赛整理的一套 Codex Skill，目前的版本是 1.0.1。

我自己是计算机学院的研究生，平时会接触模型、代码和论文写作。做这个项目时，我最关心的不是让 AI 一次生成一篇看起来很完整的论文，而是让它真正参与审题、建模、写代码、检查结果和修改论文。模型要能跑，数字要找得到出处，写进论文的结论也要经得起检查。

这个 Skill 主要面向华为杯中国研究生数学建模竞赛、MathorCup 研究生组，以及研究生校赛和省赛。可以从选题开始一路用到交稿，也可以只让它帮忙处理一个公式、一段代码、一张图或者一段论文。

## 它可以帮你做什么

- 比较几道题与队伍能力是否匹配，并安排一个短时间内能跑通的小实验，避免在不合适的题目上耗太久。
- 把每个小问拆清楚，包括已知数据、要解决的问题、变量、约束、算法、检查方法和备用方案。
- 根据任务选择预测、优化、评价、机理仿真、图网络或信号图像方法，不为了显得复杂而堆算法名。
- 帮忙写和修改 Python 或 MATLAB 代码，记录每次运行使用的数据、代码、参数和结果。
- 检查数据泄漏、约束是否满足、求解器状态、敏感性、外推风险，以及图表和论文中的数字是否一致。
- 根据已经跑出来的结果写摘要和正文，也可以在提交前检查匿名、引用、AI 使用说明和最终 PDF。

如果时间很紧，它会先想办法把所有小问做出一个能运行的基础版本，再决定哪一部分最值得继续改。它不会承诺奖项，也不会把图画得多、论文写得长或者算法名字多当成水平高。

## 安装

下载或克隆这个仓库：

```text
git clone https://github.com/Tobiasyzh/graduate-math-modeling-skill.git
```

把仓库内容放到 Codex 的技能目录，并保证 `SKILL.md` 位于技能根目录。建议使用下面这个目录名：

```text
~/.codex/skills/grad-math-modeling/
```

`dist/grad-math-modeling.skill` 是已经打包并校验过的版本，适合下载、备份或分享。

## 怎么用

如果要从头开始做一道题，可以这样说：

```text
$grad-math-modeling 这是赛题、附件和剩余时间。先帮我审题并比较选题，给出两小时内能验证的小模型，然后直接开始做。
```

如果只想解决一个问题，也可以直接说清楚范围：

```text
$grad-math-modeling 检查这个 MILP 线性化是否正确，给一个反例，再用小程序验证。不要重建整个项目。
```

```text
$grad-math-modeling 检查这份预测代码有没有数据泄漏，修好以后重新运行，并说明指标为什么发生变化。
```

```text
$grad-math-modeling 只根据现有结果文件修改摘要，不要补写没有做过的实验和数字。
```

第一次使用时，最好把这些信息一起给它：比赛名称和年份、现在是备赛还是正式比赛、题目和附件、真正的截止时间、队员会什么、电脑配置，以及已经写好的代码或结果。缺少这些信息也能开始，但有些建议只能先当作临时方案。

## 先跑一个简单例子

仓库里放了一个原创的资源分配小例子。它只有三个项目，可以把全部组合都算一遍，所以很容易检查代码有没有给出真正的最优解。

```text
python scripts/make_demo.py examples/my-first-run --plot
```

这个命令会生成结果、敏感性数据和图，并把论文里的数值结论与实际运行结果对应起来。例子中，完整枚举得到的收益是 10，按单位收益比贪心得到的收益是 7。这个例子只是用来演示工作过程，不代表某种算法在比赛中一定更好。

已有项目可以直接使用，不需要重新建目录。如果想新建一个练习项目，可以运行：

```text
python scripts/init_project.py <project-dir> --name "队伍练习" --mode practice --language python
```

## 仓库里有什么

| 路径 | 内容 |
|---|---|
| `SKILL.md` | Skill 的主要说明和工作步骤 |
| `references/` | 选题、建模、验证、画图、论文和比赛规则说明 |
| `references/algorithms/` | 六类常见任务的算法选择与检查方法 |
| `scripts/` | 初始化、运行实验、检查项目、画图和整理交稿文件的工具 |
| `assets/` | 论文大纲和资源分配练习 |
| `tests/` | 14 项工具测试和三组模拟任务 |
| `docs/` | 开发过程、测试结果和目前的限制 |
| `dist/` | 打包后的 Skill 和文件哈希清单 |

具体入口在 [SKILL.md](SKILL.md)。如果想知道某类问题该选什么方法，可以看 [references/model-routing.md](references/model-routing.md)。比赛规则和 AI 使用限制放在 [references/competition-rules.md](references/competition-rules.md)。

## 我做过的检查

仓库里的工具目前有 14 项集成测试。我在 Python 3.12.7 下运行过，结果是 14 项全部通过。测试内容包括程序运行失败、输出文件缺失、NaN 和 Infinity、路径跑出项目目录、输入或代码变化后旧结果失效，以及交稿文件被改动后的检查。

运行测试：

```text
python tests/test_tools.py
```

我还准备了三组模拟任务，用来检查选题、公式核验和论文数字修改。前两组完成了启用和不启用 Skill 的对照，两边都通过了 13 条检查。这至少说明 Skill 没有破坏原来的解题能力，但任务数量还太少，不能据此说它一定能提高成绩。第三组遇到了传输中断，所以没有计分。详细记录在 [开发与验证说明](docs/DEVELOPMENT_AND_VALIDATION.md)。

发布包的 SHA-256 是：

```text
f8287fbe4cd0a4d766f3852f7ba22ff0a80d742c9343ac88f384721805568cb8
```

## 比赛时需要注意

比赛规则每年都可能改，特别是 AI 使用范围、匿名要求、论文模板、附件格式和提交时间。这个仓库保存的是开发时查到的资料，正式比赛时还是要重新打开主办方当年的通知。

不要把没有公开的赛题、个人数据、医疗数据或其他队伍的作品上传到没有得到授权的网站。用过哪些 AI 工具、做了什么，也应该按照比赛要求如实记录。求解器显示成功，只能说明它成功求解了你输入的模型，不能反过来证明模型和公式一定写对了。

## 资料来源

开发这个 Skill 时，我读了三套公开的数学建模 Skill，也查了华为杯和 MathorCup 的官方材料。我另外精读了七篇能够核对队号、奖项和论文全文的获奖作品，并保留了一篇证据相对不完整的补充样本。

我没有把这些论文的原始 PDF、获奖名单或第三方代码放进仓库。仓库里只保留我整理的方法、来源位置和自己写的脚本。具体来源可以看 [references/sources.md](references/sources.md) 和 [references/source-catalog.json](references/source-catalog.json)。获奖论文适合学习别人怎么拆问题、做验证和写论文，但不能直接复制，也不能证明照着某种方法做就一定能获奖。

## 许可证

这个仓库目前还没有添加开源许可证。公开可见不等于允许随意复制、修改或重新发布。如果以后决定开放这些权限，我会再选择合适的许可证。
